#include <stdio.h>

int main(){

    //como el tamaño es menor el guarda a partir de cero hasta el tamaño de la cadena y despues guarda ceros
    char name[20] = "Manuel"; 

    printf("%s.", name); 

    return 0;
}